def inv_matrix(m, n):
    """
    >>> m = [[1, 2],
    ...      [3, 4]]
    >>> inv_matrix(m, 2)
    [[1, 2], [3, 4]]
    2

    :param m:
    :param n:
    :return:
    """

    # Determinant function

    # if gcd(determinant, n) is not 1, return false

    # calculate matrix inverse and mod inverse of determinant

    # multiply matrix inverse with mod inverse and take (mod n) to get positive values. return inv m (mod n)

    print m
    print len(m)
